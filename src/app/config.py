class Config:
    def __init__(self, flags):
        self.flags = flags
        self.fib = flags.fib
        self.collatz = flags.collatz
        self.title = flags.title
        self.debug = flags.debug
